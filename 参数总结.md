# 雷达抗干扰系统参数手册

本文档总结了雷达抗干扰系统中所有关键参数的定义、意义和典型取值范围。

## 一、雷达环境参数

雷达环境参数定义了雷达系统的基本特性和仿真场景。

| 参数名 | 类型 | 默认值 | 单位 | 描述 | 典型取值范围 |
|--------|------|--------|------|------|-------------|
| `f0` | float | 15e6 | Hz | 雷达中心频率（载频） | 1e6 ~ 100e6 Hz |
| `Bw` | float | 5e6 | Hz | 雷达信号带宽（LFM调频斜率） | 1e6 ~ 50e6 Hz |
| `Pw` | float | 20e-6 | s | 雷达脉冲宽度 | 1e-6 ~ 100e-6 s |
| `Fs` | float | 50e6 | Hz | 采样率（必须满足奈奎斯特准则） | ≥ 2×(f0 + Bw/2) |
| `M` | int | 1 | - | 脉冲数（慢时间维） | 1 ~ 100 |
| `N` | int | 100e-6×50e6=5000 | - | 每个脉冲的采样点数 | 1000 ~ 10000 |
| `target_dist` | float | 6000 | m | 目标距离 | 1000 ~ 10000 m |
| `target_amp` | float | 1.0 | - | 目标信号幅度（归一化） | 0.1 ~ 5.0 |
| `jammer_amp` | float | 8.0 | - | 干扰信号幅度（相对于目标） | 0.1 ~ 20.0 |

### 派生参数（自动计算）

| 参数名 | 计算公式 | 描述 |
|--------|----------|------|
| `Ts` | 1/Fs | 采样间隔时间 |
| `Npw` | int(Pw/Ts) | 脉冲宽度对应的采样点数 |
| `target_idx` | int((target_dist×2/3e8)/Ts) + Npw//2 | 目标在距离像中的索引位置 |
| `K` | Bw/Pw | LFM调频斜率（Hz/s） |

## 二、干扰信号参数

### 1. FMZuse（噪声调频阻塞干扰）
**类名：** `FMZuse`

| 参数 | 类型 | 默认值 | 单位 | 描述 | 典型取值范围 |
|------|------|--------|------|------|-------------|
| **初始化参数** |
| `C` | float | 3e8 | m/s | 光速 | 3e8 m/s (固定) |
| `f0` | float | 50e6 | Hz | 中心频率 | 1e6 ~ 100e6 Hz |
| `T` | float | 24e-6 | s | 脉宽/采样时间 | 10e-6 ~ 100e-6 s |
| `Tr` | float | 100e-6 | s | 脉冲重复周期 | 50e-6 ~ 200e-6 s |
| `B` | float | 20e6 | Hz | 信号带宽 | 1e6 ~ 50e6 Hz |
| **生成参数** |
| `R_target` | float | - | m | 目标距离（必需） | 1000 ~ 10000 m |
| `JSR_dB` | float | 8 | dB | 干信比（干扰与信号功率比） | 0 ~ 20 dB |
| `noise_var` | float | 0.1 | - | 白噪声方差系数 | 0.01 ~ 1.0 |

### 2. RGPO（距离门拖引干扰）
**类名：** `RGPO`

| 参数 | 类型 | 默认值 | 单位 | 描述 |
|------|------|--------|------|------|
| **初始化参数** |
| `C` | float | 3e8 | m/s | 光速 |
| `f0` | float | 15e6 | Hz | 中心频率 |
| `T` | float | 24e-6 | s | 脉宽/采样时间 |
| `Tr` | float | 100e-6 | s | 脉冲重复周期 |
| `B` | float | 5e6 | Hz | 信号带宽 |
| **生成参数** |
| `R_target` | float | - | m | 目标距离（必需） |
| `JSR_dB` | float | 10 | dB | 干信比 |
| `noise_var` | float | 0.1 | - | 白噪声方差系数 |

### 3. ISDJ（间歇采样直接转发干扰）
**类名：** `ISDJ`

| 参数 | 类型 | 默认值 | 单位 | 描述 |
|------|------|--------|------|------|
| **初始化参数** |
| `C` | float | 3e8 | m/s | 光速 |
| `f0` | float | 15e6 | Hz | 中心频率 |
| `T` | float | 24e-6 | s | 脉宽/采样时间 |
| `Tr` | float | 100e-6 | s | 脉冲重复周期 |
| `B` | float | 5e6 | Hz | 信号带宽 |
| **生成参数** |
| `R_target` | float | - | m | 目标距离（必需） |
| `JSR_dB` | float | 10 | dB | 干信比 |
| `noise_var` | float | 0.1 | - | 白噪声方差系数 |

### 4. SMSP（移频调制干扰）
**类名：** `SMSP`

| 参数 | 类型 | 默认值 | 单位 | 描述 |
|------|------|--------|------|------|
| **初始化参数** |
| `C` | float | 3e8 | m/s | 光速 |
| `f0` | float | 15e6 | Hz | 中心频率 |
| `T` | float | 24e-6 | s | 脉宽/采样时间 |
| `Tr` | float | 100e-6 | s | 脉冲重复周期 |
| `B` | float | 5e6 | Hz | 信号带宽 |
| **生成参数** |
| `R_target` | float | - | m | 目标距离（必需） |
| `JSR_dB` | float | 10 | dB | 干信比 |
| `noise_var` | float | 0.1 | - | 白噪声方差系数 |

### 5. NoiseProductJamming（噪声乘积干扰）
**类名：** `NoiseProductJamming`

| 参数 | 类型 | 默认值 | 单位 | 描述 |
|------|------|--------|------|------|
| **初始化参数** |
| `C` | float | 3e8 | m/s | 光速 |
| `f0` | float | 15e6 | Hz | 中心频率 |
| `T` | float | 24e-6 | s | 脉宽/采样时间 |
| `Tr` | float | 100e-6 | s | 脉冲重复周期 |
| `B` | float | 5e6 | Hz | 信号带宽 |
| **生成参数** |
| `R_target` | float | - | m | 目标距离（必需） |
| `JSR_dB` | float | 10 | dB | 干信比 |
| `noise_var` | float | 0.1 | - | 白噪声方差系数 |

### 6. NoiseConvolutionJamming（噪声卷积干扰）
**类名：** `NoiseConvolutionJamming`

| 参数 | 类型 | 默认值 | 单位 | 描述 |
|------|------|--------|------|------|
| **初始化参数** |
| `C` | float | 3e8 | m/s | 光速 |
| `f0` | float | 15e6 | Hz | 中心频率 |
| `T` | float | 24e-6 | s | 脉宽/采样时间 |
| `Tr` | float | 100e-6 | s | 脉冲重复周期 |
| `B` | float | 5e6 | Hz | 信号带宽 |
| **生成参数** |
| `R_target` | float | - | m | 目标距离（必需） |
| `JSR_dB` | float | 10 | dB | 干信比 |
| `noise_var` | float | 0.1 | - | 白噪声方差系数 |

### 7. FMNoiseSaopin（扫频噪声干扰）
**类名：** `FMNoiseSaopin`

| 参数 | 类型 | 默认值 | 单位 | 描述 |
|------|------|--------|------|------|
| **初始化参数** |
| `C` | float | 3e8 | m/s | 光速 |
| `f0` | float | 15e6 | Hz | 中心频率 |
| `T` | float | 24e-6 | s | 脉宽/采样时间 |
| `Tr` | float | 100e-6 | s | 脉冲重复周期 |
| `B` | float | 5e6 | Hz | 信号带宽 |
| **生成参数** |
| `R_target` | float | - | m | 目标距离（必需） |
| `JSR_dB` | float | 10 | dB | 干信比 |
| `noise_var` | float | 0.1 | - | 白噪声方差系数 |

### 8. FMNoiseAimedJam（瞄准式噪声干扰）
**类名：** `FMNoiseAimedJam`

| 参数 | 类型 | 默认值 | 单位 | 描述 |
|------|------|--------|------|------|
| **初始化参数** |
| `C` | float | 3e8 | m/s | 光速 |
| `f0` | float | 15e6 | Hz | 中心频率 |
| `T` | float | 24e-6 | s | 脉宽/采样时间 |
| `Tr` | float | 100e-6 | s | 脉冲重复周期 |
| `B` | float | 5e6 | Hz | 信号带宽 |
| **生成参数** |
| `R_target` | float | - | m | 目标距离（必需） |
| `JSR_dB` | float | 10 | dB | 干信比 |
| `noise_var` | float | 0.1 | - | 白噪声方差系数 |

### 9. AMNoiseGaiJam（压制式噪声干扰）
**类名：** `AMNoiseGaiJam`

| 参数 | 类型 | 默认值 | 单位 | 描述 |
|------|------|--------|------|------|
| **初始化参数** |
| `C` | float | 3e8 | m/s | 光速 |
| `f0` | float | 15e6 | Hz | 中心频率 |
| `T` | float | 24e-6 | s | 脉宽/采样时间 |
| `Tr` | float | 100e-6 | s | 脉冲重复周期 |
| `B` | float | 5e6 | Hz | 信号带宽 |
| **生成参数** |
| `R_target` | float | - | m | 目标距离（必需） |
| `JSR_dB` | float | 10 | dB | 干信比 |
| `noise_var` | float | 0.1 | - | 白噪声方差系数 |

## 三、抗干扰算法参数

### 1. WLN（宽-限-窄滤波器）
**函数名：** `WLN`

| 参数 | 类型 | 默认值 | 描述 | 典型取值范围 |
|------|------|--------|------|-------------|
| `par1` | float | 0.6 | 限幅因子（VL = par1 × 1.48），控制非线性限幅阈值 | 0.1 ~ 3.0 |
| `par2` | int | 6 | 滤波器阶数，影响带通滤波器的陡峭程度 | 2 ~ 12 |

**处理流程：**
1. **宽滤波**：宽带带通滤波器（带宽 = 2×Bw）提取信号
2. **限幅**：非线性限幅，切除幅度超过阈值VL的部分
3. **窄滤波**：窄带带通滤波器（带宽 = 1×Bw）清理互调杂波

### 2. FrequencyDomainCanceller（频域对消器）
**类名：** `FrequencyDomainCanceller`

| 参数 | 类型 | 默认值 | 描述 | 典型取值范围 |
|------|------|--------|------|-------------|
| `use_fitted_freq` | bool | True | 是否使用相位拟合估计的载频（True）或固定值（False） | True/False |
| `f0` | float | 40e6 | 固定载频（当use_fitted_freq=False时使用） | 1e6 ~ 100e6 Hz |

**算法原理：**
1. 计算信号的对数虚部（缠绕相位）
2. 解缠绕并线性拟合估计干扰载频和初相
3. 构建干扰复指数并在频域对消
4. 适用于噪声调幅类干扰的抑制

### 3. adapt_filter（自适应滤波器）
**函数名：** `adapt_filter`

| 参数 | 类型 | 默认值 | 描述 | 典型取值范围 |
|------|------|--------|------|-------------|
| `par1` | float | 0.0 | 正则化因子，添加到分母中以稳定逆运算 | 0.0 ~ 1.0 |
| `par2` | - | None | 备用参数 | - |

**算法原理：**
1. 基于信号子空间投影理论
2. 构建发射信号参考模板的投影矩阵
3. 将接收信号投影到信号子空间，抑制干扰分量
4. 适用于已知发射波形的匹配滤波场景

### 4. wave_agile（波形捷变雷达）
**类名：** `WaveAgileRadar`

| 参数 | 类型 | 默认值 | 描述 |
|------|------|--------|------|
| `PRF` | float | - | 脉冲重复频率（必需参数） |
| 其他参数待补充 | - | - | 需要查看具体实现 |

**算法原理：**
1. 通过快速改变发射波形参数（如调频斜率、脉冲宽度）
2. 使干扰机无法准确复制或预测雷达信号
3. 提高雷达在干扰环境下的生存能力

### 5. Frequency_agile（频率捷变雷达）
**类名：** `FrequencyAgileRadar`

| 参数 | 类型 | 默认值 | 描述 | 典型取值范围 |
|------|------|--------|------|-------------|
| `PRF` | float | - | 脉冲重复频率（必需参数） | 1000 ~ 10000 Hz |
| `Pw` | float | - | 脉冲宽度（应与雷达参数匹配） | 10e-6 ~ 100e-6 s |
| `Bw` | float | - | 信号带宽（应与雷达参数匹配） | 1e6 ~ 50e6 Hz |
| `Range` | float | - | 最大探测距离 | 1000 ~ 20000 m |
| `Rmin` | float | - | 最小探测距离 | 100 ~ 1000 m |
| `Rmax` | float | - | 最大探测距离 | 5000 ~ 50000 m |
| `PulseNum` | int | - | 脉冲数 | 1 ~ 100 |
| `Nwid` | int | - | 频率变化种类数 | 2 ~ 10 |
| `f0` | float | - | 中心频率（应与雷达参数匹配） | 1e6 ~ 100e6 Hz |
| `fc` | float | - | 载波频率集合 | 1e6 ~ 100e6 Hz |
| `Vt` | float | - | 目标速度 | 0 ~ 1000 m/s |
| `t0` | float | - | 起始时间 | 0 ~ 1 s |
| `Npw` | int | - | 脉宽对应的采样点数 | 100 ~ 5000 |
| `kfm` | float | - | 调频斜率 | 1e11 ~ 1e13 Hz/s |

**算法原理：**
1. 脉冲间快速改变载波频率
2. 使瞄准式干扰失效
3. 提高雷达抗干扰能力

### 6. frft_filter（分数阶傅里叶变换滤波器）
**函数名：** `frft_anti_jamming`

| 参数 | 类型 | 默认值 | 描述 | 典型取值范围 |
|------|------|--------|------|-------------|
| `a1` | float | - | 第一个脉冲的FrFT分数阶阶数 | 0.0 ~ 2.0 |
| `a2` | float | - | 第二个脉冲的FrFT分数阶阶数 | 0.0 ~ 2.0 |
| `w` | int | 100 | 掩膜宽度（用于时频域滤波） | 50 ~ 200 |

**算法原理：**
1. 利用分数阶傅里叶变换将信号旋转到最优分数阶域
2. 在分数阶域实现干扰与目标的有效分离
3. 适用于线性调频类信号的干扰抑制

### 7. qpzh（切片重组抗干扰）
**类名：** `SliceCombineJam`

| 参数 | 类型 | 默认值 | 描述 | 典型取值范围 |
|------|------|--------|------|-------------|
| `m` | int | 4 | 信号截取段数 | 2 ~ 8 |
| `n` | int | 3 | 每个子脉冲复制次数 | 2 ~ 6 |

**算法原理：**
1. 将信号在时域或频域切片
2. 根据干扰特征重组切片
3. 实现干扰抑制和目标增强

### 8. FastSlowTimeProcessor（快慢时间处理器）
**类名：** `FastSlowTimeProcessor`

| 参数 | 类型 | 默认值 | 描述 | 典型取值范围 |
|------|------|--------|------|-------------|
| `num_pulses` | int | - | 脉冲数（慢时间维） | 1 ~ 100 |
| `num_samples` | int | - | 每个脉冲的采样点数（快时间维） | 1000 ~ 10000 |
| `limit_factor` | float | 3.0 | 限幅因子（用于非线性处理） | 1.0 ~ 10.0 |

**算法原理：**
1. 同时在快时间（距离维）和慢时间（脉冲维）处理
2. 利用干扰在时域和空域的相关性差异
3. 实现多维干扰抑制

## 四、评估器参数

**类名：** `UnifiedEvaluator`

| 参数 | 类型 | 默认值 | 描述 | 典型取值范围 |
|------|------|--------|------|-------------|
| `guard_cells` | int | 4 | CFAR保护单元数，防止目标能量泄漏到参考窗 | 2 ~ 10 |
| `ref_cells` | int | 20 | CFAR参考单元数，用于估计背景噪声功率 | 10 ~ 40 |
| `Pfa` | float | 1e-5 | 虚警概率，影响CFAR检测阈值 | 1e-6 ~ 1e-3 |
| `w1` | float | 0.5 | 检测性能权重（在Reward计算中） | 0.0 ~ 1.0 |
| `w2` | float | 0.5 | SINR性能权重（在Reward计算中） | 0.0 ~ 1.0 |

**评估指标：**
1. **检测概率（R_Ed）**：目标是否被CFAR检测到（0或1）
2. **信干噪比（SINR）**：目标峰值功率与背景干扰功率之比（dB）
3. **综合奖励（Reward）**：Reward = w1×R_Ed + w2×R_ESINR，其中R_ESINR为归一化SINR分数

## 五、参数调优建议

### 雷达环境参数
1. **f0和Bw**：根据雷达实际工作频段设置，避免与其他系统干扰
2. **Fs**：必须满足采样定理，Fs ≥ 2×(f0 + Bw/2)，建议留有20%余量
3. **jammer_amp**：根据实际干扰强度设置，典型值4.0~12.0对应12~22dB JSR

### 干扰参数
1. **JSR_dB**：干信比，控制干扰强度，典型值8~15dB
2. **噪声方差**：控制背景噪声水平，典型值0.05~0.2

### 抗干扰参数
1. **WLN的par1**：限幅因子，值过小会损伤目标信号，值过大会残留干扰，建议1.5~3.0
2. **WLN的par2**：滤波器阶数，阶数越高滤波越陡峭但计算量越大，建议4~8
3. **自适应滤波的par1**：正则化因子，防止矩阵奇异，建议0.01~0.1

### 评估器参数
1. **CFAR参数**：根据雷达距离分辨率调整，保护单元应覆盖目标扩展
2. **权重w1、w2**：根据任务需求调整，重视检测则增大w1，重视信噪比则增大w2

## 六、典型配置示例

```python
# 雷达环境典型配置
radar_params = {
    'f0': 15e6,         # 15MHz中心频率
    'Bw': 5e6,          # 5MHz带宽
    'Pw': 20e-6,        # 20μs脉宽
    'Fs': 50e6,         # 50MHz采样率
    'target_dist': 6000,# 6km目标距离
    'jammer_amp': 8.0,  # 强干扰环境
}

# WLN抗干扰典型参数
antijam_kwargs = {
    'par1': 2.5,        # 中等限幅强度
    'par2': 6,          # 6阶滤波器
}

# 评估器典型参数
evaluator_params = {
    'guard_cells': 4,
    'ref_cells': 20,
    'Pfa': 1e-4,
}
```

## 七、注意事项

1. **参数单位**：所有时间参数单位为秒（s），频率参数单位为赫兹（Hz）
2. **参数一致性**：干扰和抗干扰参数需要与雷达环境参数匹配
3. **计算复杂度**：高阶滤波器和复杂算法会增加计算时间
4. **实际应用**：实验室仿真参数可能需要根据实际雷达系统调整

## 八、更新说明（基于代码详细检查）

### 干扰参数补充：
1. **通用初始化参数**：所有干扰类共享 `C`, `f0`, `T`, `Tr`, `B` 参数
2. **RGPO特有参数**：`N_pulses` (默认16，脉冲数用于构造拖引时序)
3. **qpzh特有参数**：`m` (信号截取段数，默认4)，`n` (每个子脉冲复制次数，默认3)
4. **生成方法通用参数**：所有干扰的 `generate()` 方法都需要 `R_target`, `JSR_dB`, `noise_var`

### 抗干扰参数补充：
1. **wave_agile**：需要 `PRF`, `Pw`, `Bw`, `Range`, `Rmin`, `Rmax`, `PulseNum`, `Nwid`, `f0`, `Vt`, `t0`, `Npw`, `kfm` 等参数
2. **Frequency_agile**：类似wave_agile，额外需要 `fc` 参数
3. **frft_filter**：`a1`, `a2` (相邻脉冲的FrFT分数阶阶数)，`w` (掩膜宽度，默认100)
4. **qpzh**：作为抗干扰时，参数与干扰模式相同
5. **FastSlowTimeProcessor**：`num_pulses`, `num_samples`, `limit_factor` (限幅因子，默认3.0)

### 参数关联性：
1. **雷达环境参数** → **干扰参数**：干扰的 `f0`, `B`, `T` 应与雷达参数匹配
2. **干扰参数** → **抗干扰参数**：抗干扰算法需要了解干扰特性进行优化
3. **统一框架**：`unified_framework.py` 提供参数适配层，自动处理参数传递

## 九、无干扰仿真参数

### 无干扰场景配置
当 `jammer_type` 设置为以下值时，系统将运行无干扰仿真：
- `None`
- `'NoJammer'`
- `'no_jammer'`
- `''`（空字符串）

### 无干扰仿真参数
| 参数名 | 类型 | 默认值 | 单位 | 描述 | 典型取值范围 |
|--------|------|--------|------|------|-------------|
| `noise_level` | float | 0.5 | - | 高斯白噪声幅度系数 | 0.1 ~ 2.0 |
| `target_amp` | float | 1.0 | - | 目标信号幅度（归一化） | 0.1 ~ 5.0 |

### 无干扰仿真流程
1. **信号生成**：调用 `RadarEnvironment.generate_without_jammer(noise_level=0.5)`
2. **抗干扰处理**：仍可应用抗干扰算法（用于测试算法对纯目标+噪声的性能）
3. **性能基准**：无干扰情况提供性能基准，用于评估抗干扰算法本身引入的损失

### 无干扰性能期望
| 指标 | 典型范围 | 说明 |
|------|----------|------|
| **SINR (无干扰)** | 10 ~ 20 dB | 纯目标+噪声环境下的信噪比 |
| **检测概率** | 接近 1.0 | 无干扰时CFAR应能可靠检测目标 |
| **Reward (无干扰)** | 0.6 ~ 0.9 | 取决于评估器权重配置 |

### 使用示例
```python
# 无干扰仿真
results = run_simulation(
    jammer_type=None,  # 或 'NoJammer'
    antijam_type='WLN',
    radar_params={'f0': 15e6, 'Bw': 5e6, ...},
    antijam_kwargs={'par1': 2.5, 'par2': 6}
)

# 无干扰作为性能基准
no_jammer_results = run_simulation(
    jammer_type='NoJammer',
    antijam_type='None',  # 不使用抗干扰，评估原始性能
    radar_params=custom_radar_params
)
```

---



*文档版本：1.2*  
*更新日期：2026-03-18*  
*基于代码版本：unified_framework.py (v1.0) 及详细模块检查*  
*更新内容：补充抗干扰算法详细参数、添加无干扰仿真参数章节*