# 000 协作闭环约定执行报告

## 1. 任务结论

已将 ChatGPT ↔ Codex 协作闭环写入仓库根目录 `AGENTS.md`，作为长期项目规则。

## 2. 实际修改内容

- 增加角色分工和共享文件约定。
- 增加任务启动前的阅读、调用链和 shape 检查流程。
- 增加任务文件最低内容要求。
- 增加最小修改原则、完成后的报告要求和审查门禁。
- 增加分支、commit、diff 和测试记录约定。

## 3. 修改文件列表

- `AGENTS.md`
- `docs/reports/000-collaboration-protocol_result.md`

## 4. 关键代码逻辑

本次没有代码或算法修改，仅更新协作流程文档。

## 5. 与任务设计的对应关系

已落实任务中约定的 `AGENTS.md`、`ROADMAP.md`、`docs/tasks/`、`docs/reports/` 四类共享文件和“ChatGPT 审查通过前不进入下一任务、不开始长训练”的门禁。

## 6. 测试命令和测试结果

- `sed -n '1,220p' AGENTS.md`：通过，新增规则可读。
- `git status --short`：通过，未执行破坏性操作。

## 7. 尚未解决的问题

- 本次未创建 Git commit，等待按项目协作流程由后续审查决定是否提交。

## 8. 潜在风险

- 当前工作区已有任务 018/019 相关未提交文件；本次未修改或清理这些文件。

## 9. Git 分支和 commit ID

- 分支和 HEAD 以执行报告生成时的本地 Git 状态为准。
- 本次未产生新 commit。
